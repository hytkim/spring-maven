<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.mybatis.mapper.DeptMapper">
	<!-- 전체조회 -->
	<select id="selectAll" parameterType="DeptVO">
		SELECT
			department_id
			, department_name
			, manager_id
			, location_id
		FROM departments
		ORDER BY department_id
	</select>
	
	<!-- 단건조회 -->
	<select id="selectInfo" parameterType="DeptVO">
		SELECT
			department_id
			, department_name
			, manager_id
			, location_id
		FROM departments
		WHERE department_id = #{departmentId}
	</select>
	<!--  등록 -->
	<insert id="insertInfo" parameterType="DeptVO">
		Insert INTO departments
					(
						department_id
						, department_name
					)
					VALUES
					(
						DEPARTMENTS_SEQ.NEXTVAL
						, #{departmentName}
					)
	</insert>
	<!--  수정 -->
	<update id="updateInfo" parameterType="DeptVO">
		UPDATE departments
		SET
			manager_id  = #{managerId}
			location_id = #{locationId}
		WHERE department_id = #{departmentId}
	</update>
	<!--  삭제 -->
	<delete id="deleteInfo" parameterType="int">
		DELETE FROM departments
		WHERE department_id = ${deptId}
	</delete>
</mapper>